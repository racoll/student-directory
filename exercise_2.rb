def print(students)
  if # student's name begins with '' letter...
    # run this code
  else
    # don't run the code
  end
  students.each do |student|
    puts "#{student[:name]} (#{student[:cohort]} cohort)"
  end
end
